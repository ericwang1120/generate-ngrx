import { Observable } from 'rxjs/Observable';
import { {{properCase odd}} } from '../models/{{dashCase odd}}';
import { Http, Headers, Response, RequestOptions } from '@angular/http';
import { Injectable } from '@angular/core';

@Injectable()
export class {{properCase odd}}Service {
  constructor(private http: Http) { }

  public load(): Observable<{{properCase odd}}[]> {
    return this.http
      .get(`https://api.myapi.com`, this.jwt())
      .map((res) => {
        return res.json();
      });
  }

  private jwt() {
    const jwtHeaders = new Headers({ 'Content-Type': 'application/json' });
    return new RequestOptions({ headers: jwtHeaders });
  }
}
